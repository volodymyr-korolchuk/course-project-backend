version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: course-project-container
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: course-project-db
    ports:
      - '5555:5432'
